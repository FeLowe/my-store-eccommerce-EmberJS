import { itemLimit } from 'ecommerce/helpers/item-limit';
import { module, test } from 'qunit';

module('Unit | Helper | item limit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemLimit([42]);
  assert.ok(result);
});
